# 戦略検証メモ (2026-08-17)

「本当に利益を出すか」をコードの外から数字で確かめた記録。以降の変更は必ずここに追記する。

## 実測: 何が起きていたか

ライブ (BitFlyer 現物、2026-05-28 〜 08-17):

| 指標 | 値 |
|---|---|
| 確定損益 | **-¥12,742** (取引所の約定履歴ベース) |
| 決済回数 | 175 (勝 51 / 負 124、勝率 29.1%) |
| 売買代金 | 買 ¥1,218,594 / 売 ¥1,220,157 |
| 資金 | ≈ ¥63,000 → **19 回転** |

往復コスト ≈ 0.30〜0.35% × 19 回転 = **資金の 6% 前後が手数料とスプレッドで消えている**。

決済の内訳は stop_loss 123 回に対し take_profit 6 回。これは 2026-08-16 に修正した
損切り符号バグ (`changePercent <= stopLossPercent` を反転していなかった) の痕跡で、
「1 時間後に相場がどこにいても投げる」動作になっていた。

## バックテスト: 戦略そのものが逆を向いていた

`npm run bt` (scripts/backtest-matrix.harness.ts)。
3 ペア (BTC/ETH/XRP) × 3 期間 (5y / 直近2y / 直近1y) = 9 条件。
BitFlyer taker 0.15% + スリッページ 0.05% を計上。日足、翌バー始値約定。

**全ペア × 全期間の平均リターン**

| 変種 | 平均 | 最悪 | 内容 |
|---|---|---|---|
| C 50/200 | **+24.1%** | -26.6% | 移動平均 50/200 だけ。上昇時ロング、それ以外は現金 |
| C+SL8 | +23.7% | -26.9% | 上に -8% の保険 SL を足した実務版 (ほぼ同じ = 損切りを残しても壊れない) |
| C 20/100 | +18.8% | -48.3% | 同 20/100 |
| C 100/200 | +11.8% | -27.2% | 同 100/200 |
| V2 trendGate + SL5 | +9.1% | **0.0%** | 現行 + 下降時 BUY 禁止 + SL 2%→5% |
| V1 trendGate | +2.9% | -5.5% | 現行 + 下降時 BUY 禁止 |
| V3 順張り化 | -2.6% | -31.7% | V2 + 上昇確定時は F&G 条件を免除 |
| C 30/150 | -9.5% | -65.7% | 同 30/150 |
| **V0 現行** | **-17.5%** | -25.3% | クオンツ + AI + 規律フィルタ一式 |

### 分かったこと

1. **現行戦略は 9 条件すべてでマイナス。** BTC/JPY 直近1年は 7 戦 0 勝 7 敗、平均 -4%。
   エントリー理由は毎回「F&G 恐怖域 (≤35) → 逆張り買い」。同期間の buy&hold は -43.8%。
   つまり**下降トレンドで落ちるナイフを掴み続けていた**。負けの主因は執行でもチューニングでもなく方向。

2. **F&G フィルタが逆張り専用機にしていた。** BUY には「F&G ≤ 35 (恐怖)」が必要な設計。
   上げ相場の F&G は 50〜75 なので、**上昇トレンドでは 1 本も買えない**。
   V1/V2 の取引回数 0 がその証拠。

3. **クオンツ/スコアリングのエントリー信号にエッジが無い。**
   F&G 条件を外して上昇トレンド中も買えるようにした V3 は -2.6%。
   一方、クオンツも AI も F&G も使わない移動平均だけの対照群 C は +24.1%。
   13,000 行のスタックが 30 行のルールに 40 ポイント負けている。

4. **ショートは採用しない。** 下落局面で稼ぐには論理的にショートだが、実測は:

   | | BTC 5y | ETH 5y | XRP 5y |
   |---|---|---|---|
   | 確定下降トレンドのみショート + SL8 | +152.2% | -26.4% | -53.3% |

   BTC の 1 例以外は壊滅。「MA50 割れで売る」ようにすると更に悪化 (XRP -84%)。
   強気相場の押し目を売り続けるため。**現時点で本番投入する根拠は無い。**

5. **C の +24.1% も頑健なエッジではない。** 30/150 にすると -9.5%。
   平均の大半は XRP 直近2年 (+119.9%) が作っている。パラメータ 1 つで符号が変わるものは
   エッジではなくノイズとして扱う。

### したがって、正直な結論

- **確実に言えるのは「現行のエントリー判断は有害」**ということだけ。止めれば確実に改善する。
- テストした範囲に**下げ相場で儲かる設定は無い**。現状で正しい行動は「上昇トレンドが来るまで現金で待つ」。
- 「24 時間動くから儲かる」ではなく、「24 時間動くから**逃げ遅れない / 上昇の入り口を逃さない**」が
  このアプリの現実的な価値。

## 入れた変更

### 1. 日足トレンドゲート (本番投入済み)

`src/lib/trading/trend-gate.ts` — バックテストと本番で同じ定義を使う単一の出所。

- ロング可: 日足終値 > MA50 > MA200
- ロング撤退目安: 日足終値 < MA50
- ショート可 (未使用): 日足終値 < MA200 かつ MA50 < MA200

`engine.ts` の規律フィルタ 1.2 として、**上昇トレンドでない限り新規 BUY を HOLD 化**する。
日足が取れない場合も見送り (資金を守る側に倒す)。`TREND_GATE=false` で無効化可能。
日足の取得本数を 100 → 250 に増やして MA200 を計算できるようにした。

符号規約は `trend-gate.test.ts` で固定 (5 件)。損切り符号バグの再発防止と同じ趣旨。

### 2. 検証ハーネス

- `npm run bt` — 戦略マトリクス。`vitest.harness.config.ts` で通常の `npm test` からは分離。
- `src/lib/backtest/replay.ts` に `strategy` / `trendGate` / `allowShort` / `trendFollowEntries` を追加。
  ショートは損益・約定価格・スリッページの向きを side 別に計算する。

### 3. 「なぜ買っていないのか」の可視化 (本番投入済み)

`/api/bot/status` に `trendStates` を追加し、トップ画面に「日足トレンド」パネルを出した。
ペアごとに 買える / 待機 と MA 値を表示し、全ペアが待機なら
「新規買いを見送っています。故障ではなく待機です」と明示する。
過去に「現金があるのに取引しない」を不具合として追いかけた経緯があるため、
**設計どおりの待機と不具合を画面で区別できる**ようにしておく。

2026-08-17T16:08 の本番実測: BTC 終値 10,217,900 / MA50 10,255,623 / MA200 10,998,777 → buyAllowed=false。
5 ペアすべて上昇トレンドではないため、現時点で新規買いは出ない。

### 4. エントリー判断を MA ルールに置換 (本番投入済み、2026-08-17)

`ENTRY_MODE=trend` (既定) で、入る/出るの判断を日足 MA ルールに任せる。
クオンツ・AI・F&G・EV ゲートはエントリー判断から降ろした (監査ログと学習系はそのまま動く)。

- 保有なし × 日足 終値 > MA50 > MA200 → BUY (SL -8% / TP 30%)
- 保有あり × 日足 終値 < MA50 → SELL (`[MAルール撤退]` は min hold 24h を通り抜ける)
- それ以外 → 何もしない。トレンド判定不能なら待機

判断は `decideByTrend()` として純関数に切り出し、`trend-gate.test.ts` 9 件で固定した
(上げ相場で BUY が出ること、下降で未保有なら手を出さないこと、撤退接頭辞が付くこと)。
`ENTRY_MODE=quant` で旧判断に戻せる。

**残した安全装置**: SL -8%、緊急ロスカット、キルスイッチ、サーキットブレーカー、
RiskGate、クールダウン、配分上限。ダスト (< ¥500) は保有に数えない
(数えると売れない残骸でそのペアが永久に買えなくなる)。

### 5. 【重要】買っていたのは AI 判断ではなく配分リバランサーだった

MA ルール投入後にライブを監視して発覚。直近の LIVE BUY は**3 件とも `alloc-` 由来**:

```
06:58 XRP/JPY ¥12,000  alloc-1786949911811-XRPJPY
12:58 BTC/JPY ¥15,000  alloc-1786971505838-BTCJPY
21:14 ETH/JPY ¥12,000  alloc-1787001272462-ETHJPY
```

`allocation-maintainer` が「目標現金比率に戻す」ために、AI 判断とは無関係に買っていた。
つまり 3 と 4 で AI 判断側にゲートを入れても、**実際に買っている経路は素通り**だった。
下降トレンド中に現金を投入し続ける動きで、戦略の意図を正面から打ち消す。

→ 配分リバランスの買い候補からも上昇トレンドでないペアを除外し、発注直前にも
   もう一度トレンドを見る二重の歯止めを入れた。`trendByPair` が空なら候補ゼロ (買わない側に倒す)。

他の買い経路も確認: `GRID_ENABLED=0`、`DCA_ENABLED` 未設定 → どちらも停止中。
現時点で LIVE BUY が出るのは「MA ルール判断」と「配分リバランス」の 2 経路のみで、
どちらも同じトレンド条件を通る。

**教訓**: 判断ロジックを直す前に、**実際に発注しているのがどの経路か**を約定 ID で確認する。

### 6. ダッシュボードの実害 3 件 (本番投入済み、2026-08-18)

画面を疑って調べたら、取引ロジックとは別に実在する不具合が 3 件あった。

1. **`SOL/JPY` の ticker が毎回 HTTP 500**
   `BITFLYER_PAIRS` に SOL/JPY が入っていたが BitFlyer に存在しない (ccxt が BadSymbol)。
   画面がポーリングのたびに 500 を出し続けていた。→ 銘柄リストから除去。

2. **`/api/trades` の戻り値を画面が読めていなかった**
   API は `{ trades: [...] }` を返すのに、画面側は `Array.isArray(t) ? t : []` で読んでいた。
   配列ではないので常に空配列になり、**ダッシュボードの約定履歴はずっと空**だった。
   しかもデータ自体は毎回ダウンロードして捨てていた。

3. **1 回のポーリングで 522KB**
   1 レコードに AI 各エンジンの判断全文 (`aiDecision.engineResults`) が入っていたが、
   画面で `aiDecision` を参照している箇所はゼロ。→ 既定で除外し **522KB → 61KB (88%減)**。
   `full=1` で完全版、`limit=0` で全件 (履歴ページ用)。

あわせてポーリング間隔を 15 秒 → 30 秒に (`adaptive-strategy` 10.6s / `diagnostics` 7.6s /
`status` 3s と、1 周が 15 秒に収まらないため)。

本番で確認済み: ticker リクエストが 6 本 → 5 本 (SOL 消滅)、`/api/trades?limit=200` が
60KB で `aiDecision` を含まない。

⚠️ **判断を誤った点の記録**: 当初これらを「ページが固まっている」根拠として調べ始めたが、
ブラウザ拡張のスクリーンショットは `example.com` でも同じエラーを出した。
固まっていたのは拡張であってアプリではない。**画面の見え方は未確認のまま**。
ツールの失敗をアプリの不具合と読み違えないこと。

### 7. MA ルールの買いを黙って潰す 3 つの経路 (本番投入済み、2026-08-19)

画面の「失敗からの学習」を読んで気づいた。MA ルールは上昇トレンドでしか買わないのに、
学習側は「TRENDING_UP 局面が損失の 80%」という教訓を持っている。
これは**損切り符号バグの時期に出た損失**を集計したもので、今の戦略への教訓ではない。
本番の active な学習ルールを実際に見たら、そのまま効いていた。

```
regime::BUY::TRENDING_UP::conf70   「quant/technical/intel の 2 つ以上が HOLD か SELL なら買うな」
timing::BUY::TRENDING_UP::conf80   同上
```

MA ルールはクオンツの同意を必要としない設計なので、この条件は日常的に成立する。
そして `matchLessons` の `check.blocked` は**確信度に関係なく無条件 return** していた。
つまり相場が上昇に転じた最初の買いが、黙って消える構造だった。

さらに致命的だったのが SL/TP。MA ルールは SL -8% を指定していたが、
建玉時に `classifyPositionStyle` が上書きしていた。

| style | TP | SL |
|---|---|---|
| SCALP | 1.2% | **0.6%** |
| SWING | 8% | 2% |
| HOLD | 30% | 8% |

SCALP 判定なら **SL 0.6%**。往復コスト 0.3% に対して薄すぎ、
バックテストが否定した「ノイズで刈られる」構造そのものになる。
**検証した設定 (C+SL8) と実際に執行される設定が違っていた。**

対応:
- 適応ガードレールと学習ルールは `ENTRY_MODE=trend` の間は**警告のみ**にした
  (記録・表示・学習は継続。拒否権だけ外す)。
- MA ルールで入った玉は style 分類を適用せず、SL -8% / TP 30% / 部分利確なしに固定。

**教訓**: 「検証した設定」と「実際に執行される設定」が一致しているかを、
建玉のコードまで追って確認する。決定を出す側だけ直しても意味がない
(上記 5 の「買っていたのは別経路だった」と同じ失敗の繰り返し)。

⚠️ 未整理: retrospective の `slMultiplier` / `confidenceBonus` (現在 +8、XRP +12) が
MA ルール経路にどう効くかは未検証。確信度 75 は閾値 (50+12=62) を超えるので
今のところ通るが、bonus が 26 以上になると買えなくなる。

### 8. 検証を本番の実挙動に合わせたら、XLM/MONA が有害だと分かった (2026-08-19)

これまでの検証は「1 ペアに全資金」だった。本番は「各ペアに固定額 (¥15,000) を独立に張る」。
**検証した設定と動いている設定が違う**という同じ罠を 3 回踏んだので、
ポートフォリオ検証を追加した (`npm run bt` の「ポートフォリオ」テスト)。
各ペアを資金 1/N で独立に回し、日次の合計エクイティを積む。

| | 5y全期間 | 直近2y | 直近1y |
|---|---|---|---|
| 3ペア (BTC/ETH/XRP) | **+46.2%** (DD 31.3%) | **+47.2%** (DD 42.3%) | -18.8% (DD 18.7%) |
| 5ペア (+XLM/MONA) | +4.6% (DD 34.9%) | +15.9% (DD 42.3%) | -21.4% (DD 21.3%) |

ペア別の内訳:

```
BTC   5y +133.1%   2y  +22.9%   1y -16.5%
ETH   5y   -6.1%   2y   -0.3%   1y -23.9%
XRP   5y  +11.6%   2y +119.0%   1y -16.1%
XLM   5y  -44.5%   2y  -32.3%   1y -45.2%   ← 全期間で負け
MONA  5y  -71.2%   2y  -29.9%   1y  -5.4%   ← 全期間で負け
```

MA ルールを使っても XLM と MONA は全期間で損失。5 年で **41 ポイント**削っている。
本番の損失分析も独立に「XLM/JPY が損失の 31% (¥-8,572)」と言っており、2 方向の証拠が一致。

→ Railway の `TRADING_PAIRS` を `BTC/JPY,ETH/JPY,XRP/JPY` に変更。
   コードの既定値も元から 3 ペアで、コメントに「流動性高めの主要ペアのみ」とあった。
   env で 5 ペアに広げられていたのが実態。

⚠️ 期間は入れ子 (5y ⊃ 2y ⊃ 1y) なので独立したサンプルではない。
   そして**直近 1y は 3 ペアでも -18.8%**。今はその局面にいる。

### 9. 稼働コストの削減 (2026-08-19)

MA ルールは AI の判断を使わないのに、毎サイクル 5 ペア分の LLM を呼んでいた
(1 日 120〜360 回)。判断に使わない出力への課金は、そのまま損益を削る。
→ 「MA ルールの結論が HOLD = 何もしない」サイクルでは AI 照会を省略。
   売買するサイクルでは従来どおり呼び、監査ログと学習の材料を残す。
   `SKIP_AI_WHEN_IDLE=false` で無効化可。

本番確認: `engineResults` が `[('claude','success','...AI 照会を省略 (課金抑制)')]` になっている。

### 10. その他の修正 (2026-08-19)

- **確信度閾値**: `effectiveThreshold = 50 + confidenceBonus`。retrospective が bonus を
  自動調整しており、+26 以上になると MA ルールの確信度 75 が閾値を下回って買えなくなる。
  MA ルールの 75 は勝率推定ではなく固定値なので、閾値比較の対象から外した。
- **4h 足**: `getOHLCV(pair,"4h",n)` が aggregate 未指定で 1 時間足を返していた。
  CryptoCompare の `aggregate` と BitFlyer chart API の `grouping` を渡すよう修正。
  実測で 1h=1.0h / 4h=4.0h / 1d=24.0h を確認。
- **サイズの誤報**: 「配分 ¥1,402 では最小注文額に届かず買えない」と書いたが誤り。
  ライブは最後に `Math.max(..., LIVE_BASE_TRADE_JPY=¥15,000)` の下限が効く。
  ただし副作用として、**画面の配分と RiskGate はライブの新規買いに効いていない**。

## 決めていない論点 (解決済み: 上記 4 で MA ルールを採用)

現状の本番は「上昇トレンドでなければ買わない」。守りは効くが、
**エントリー判断そのものは今もクオンツ + F&G のまま**なので、
上昇トレンドが戻っても F&G が「恐怖 (≤35)」でなければ買えない。
上げ相場の F&G は 50〜75 なので、**次の上昇局面もほぼ見送る**ことになる。

選択肢は 2 つ。

| | 平均 | 最悪 | 性格 |
|---|---|---|---|
| 現状維持 (V2 相当) | +9.1% | **0.0%** | ほぼ取引しない。減らないが増えない |
| エントリーを MA ルールに置換 (C+SL8) | **+23.7%** | -26.9% | 上昇に乗る。下げ相場では -27% の年もある |

C は 30/150 にすると -9.5% になるのでパラメータに頑健ではない。
実弾を張る以上、どちらを選ぶかは資金の話なので独断では切り替えない。

## 未処理 / 次に効きそうなこと

- **`getOHLCV(pair, "4h", n)` が 1 時間足を返している** (`tfMap` で "4h" → "histohour"、
  aggregate 指定なし)。MTF 整合性チェックが実質 1h 対 1h を比べている。要検証・要修正。
- クオンツスタックをエントリー判断から降ろし、サイズ調整・拒否権だけに使う案。
- 方向を当てにいかない収益源 (レンジでのマーケットメイク / 取引所間裁定)。
  現状の「方向を当てる」路線は 9 条件で否定されている。
- 生成 AI 呼び出しのコストは損益計算に入っていない。BUY がゲートで止まる局面では
  そもそも AI を呼ばない方が良い。

---

# 2026-08-19 追記: コア保有 (売らない長期枠)

## きっかけになった実測

本番 `/api/bot/status` (2026-08-19T07:00 JST 16:00):

| | 値 |
|---|---|
| JPY free | ¥63,015 (used 0) |
| ポジション | BTC 0.0000057 / ETH 0.00015 / XRP 0.001 / XLM 0.008 — **全部ダスト、評価額ほぼ ¥0** |
| trendStates | BTC・ETH・XRP すべて `buyAllowed=false` |
| 累積 | -¥16,023 (-25.4%) / 200 取引 / 勝率 36.8% |

BTC 終値 10,251,285 < MA50 10,282,970 < MA200 10,975,625、
XRP 160 < 173 < 203。**トレンドゲートの設計どおりの待機**であって不具合ではない。

問題はその先。上昇トレンドが戻るまで何ヶ月でも全額 JPY で待つ構えになっていて、
bitFlyer に置いた JPY は銀行に出金しない限り生活資金として使えるわけでもない。
「待機が正しい」ことと「その資金が死んでいる」ことは両立する。

## 入れたもの

資金を 2 つの枠に分ける。

| 枠 | 誰が動かすか | 売るか | ゲート |
|---|---|---|---|
| **コア枠** | `maintainCoreHolding()` | **売らない** | トレンドゲート・kill switch の対象外 |
| 戦術枠 | MA ルール (`ENTRY_MODE=trend`) | SL/TP で売る | 従来どおり |

- 目標は「総資産 (JPY + 暗号資産評価額) の N%」。既定 70%、比重 BTC 0.6 / ETH 0.4。
- **一括では入れない**。`CORE_HOLD_TRANCHES` (既定 4) に分割し、
  同じペアは `CORE_HOLD_INTERVAL_HOURS` (既定 24h) 空ける。下降局面での一括投入を避ける。
- 1 サイクルにつき発注は最大 1 件。充足率が最も低いペアから積む。
- bitFlyer の最小注文 (BTC 0.001 ≒ ¥11,000) を下回るトランシェは最小注文まで引き上げる。
  引き上げても不足額を超えるなら見送る。

実装は `src/lib/trading/core-holding.ts` (純関数) と engine 側の配線。
判断部分は全部純関数にして `core-holding.test.ts` 25 件で固定した。

## 売却経路を全部塞いだ

過去 2 回、「判断ロジックだけ直して発注経路が素通り」を踏んでいる
(alloc 経路の見落とし / 検証した設定と執行される設定の乖離)。同じ失敗を繰り返さないため、
**取引所残高をそのまま売っていた箇所を洗い出して全部 `sellableFree()` を通した**。

| 経路 | 対応 |
|---|---|
| AI/MA ルールの SELL | `sellableNow` (コア控除済み) |
| SL / TP (`closeLivePositionAtExit`) | 同上 |
| 部分利確 (PTP) | 同上 |
| 高速監視ループ (`monitorPositionsFast`) | 残高からコア控除 |
| 緊急ロスカット (`emergencyLossCut`) | `cutQty` に変更 |
| kill switch の全決済 (`closeAllLivePositions`) | コアのみのペアは決済しない |
| グリッドの `marketSell` | `gridSellable` に変更 |

`holdsPosition` (MA ルールの保有判定) からもコアを除外した。
除外しないと、コアを積んだ瞬間に「保有あり」となって戦術枠が新規に買えなくなる。

## 台帳がずれたときの自己修復

コア台帳はアプリ側の記録でしかない。本人が bitFlyer の画面から直接売れば台帳だけが残り、
`sellableFree` が 0 を返し続けて**そのペアが二度と売れなくなる**。
毎サイクル実残高と突き合わせ、超過分は按分で切り詰める (`clampCoreToBalance`)。
約定記録も `order.amount` を鵜呑みにせず、直後の実残高で上限を取る。

## 画面から操作できるようにした

Railway の環境変数を書き換えないと ON/OFF も比率も変えられない状態は、
「今すぐ止めたい / 増やしたい」に応えられない。`/settings` にカードを追加し、
`/api/core` (GET/POST) 経由で保存する。保存値は env 既定に重なる (`mergeCoreConfig`)。
GET は「次に積むならこれ」を**発注せずに**返すので、ON にする前に金額を確認できる。

## 盛らないための注記

- これは**勝ちを保証する仕組みではない**。下落局面ではそのまま含み損になる。
  損切りもキルスイッチもこの枠には効かない。それを承知で持つ枠。
- バックテストで確認できているのは「現行の逆張りより確実にマシ」まで。
  **コア保有そのものの期待値は検証していない**。
- 既定は `CORE_HOLD_ENABLED` 未設定 = **停止**。実弾が動くので明示的な opt-in を要求する。

## 同日のついで修正

- `scoring-engine` の `book` (板情報) が `compositeScore` に 10% で効いているのに
  `audit.votes` に入っていなかった。監査を材料にする signal-learning / retrospective が
  板の寄与を学習できず、ウェイト合計も 0.9 にしか見えていなかった (テストが失敗し続けていた原因)。
  votes に追加してテストを実態に合わせた。
- 上の「未処理」に挙げていた **4h 足の aggregate 未指定は既に修正済み**
  (`AGGREGATE_BY_TIMEFRAME` + CryptoCompare `aggregate` / bitFlyer `grouping`)。

## 同日の追加分 (実装後に本番で分かったこと)

### 目標比率どおりに積み切れない問題 → 端数を他ペアに回す

bitFlyer の BTC は 0.001 刻み (現値で ≒¥11,265)。目標額が最小注文の整数倍から外れると、
最後の端数が永久に発注できない。既定 (70%・BTC 0.6/ETH 0.4) の実測値で言うと:

| | 目標 | 実際に積める額 |
|---|---|---|
| BTC | ¥26,516 | ¥11,265 × 2 = ¥22,530 (残 ¥3,986 は最小注文未満) |
| ETH | ¥17,677 | ¥4,419 × 4 = ¥17,677 |
| 合計 | ¥44,193 (70%) | **¥40,207 (64%)** |

「70% と指定したのに 64% で止まる」は、使う側からは故障と区別がつかない。
刻めない端数は他ペアの目標に回すようにした (`planCoreBuy` の再配分)。
回す先が無ければ従来どおり見送る (無理な発注はしない)。

### 画面が重かった原因の一部は自分で足していた

status は数秒おきにポーリングされるのに、1 リクエストの中で
「JPY 残高」と「コア保有サマリ」が別々に `getBalance` を叩き、
さらにコア側が全ペアの ticker を取りに行っていた。bitFlyer への往復は 1 回数秒かかる。

- 価格は `state.lastPriceByPair` (直近サイクル / 高速監視で記録) を使う
- 残高は 20 秒の短期キャッシュ (`getBalanceCached`)
- 発注判断 (`maintainCoreHolding`) だけは `maxAgeMs=0` で必ず実測

実測 `/api/bot/status`: **14.4s → 2.4〜2.8s**。

### ついでに直した表示バグ

- `getPositions()` が `currentPrice: 0, // updated by cycle` のまま返していて、
  **画面のポジションが常に「評価額 ¥0・含み損益 0」**だった。
  リスクオーバーレイ (`buildPortfolioRiskOverlay`) もこの 0 を使って計算していた。
  直近価格を記録して実値を返すようにした。
- 起動時の reconcile が「dust (¥58 / ¥46) → livePositions から除外」した直後に、
  同じサイクルの FIFO 再構築が作り直していた。残骸が毎サイクル復活し、
  高速監視と緊急ロスカットが毎分それをポーリングして
  「売却可能数量未満」を出し続けていた。売却可能な数量があるときだけ再構築する。
